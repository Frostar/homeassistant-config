# Stub secrets for CI only — every value here is fake.
# Must contain a key for every `!secret` reference in the tracked config.
latitude: 55.6761
longitude: 12.5683
radius: 100
db_url: "sqlite:////config/home-assistant_v2.db"
